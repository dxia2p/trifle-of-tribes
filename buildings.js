class Building{
    constructor(gridWidth, gridHeight, pos, color){
        this.gridWidth = gridWidth;
        this.gridHeight = gridHeight;
        this.pos = pos;
        this.color = color;
    }
}